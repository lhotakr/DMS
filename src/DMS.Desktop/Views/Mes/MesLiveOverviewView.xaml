<UserControl x:Class="DMS.Desktop.Views.Mes.MesLiveOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="620">
    <Grid Background="{DynamicResource DmsBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock x:Name="TxtTitle"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource DmsForegroundBrush}"/>
            <TextBlock x:Name="TxtSubtitle"
                       Margin="0,4,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,0,0,12"
                Padding="10"
                BorderThickness="1"
                BorderBrush="{DynamicResource DmsBorderBrush}"
                Background="{DynamicResource DmsPanelBrush}">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock x:Name="LblWorkcenter"
                           Margin="0,0,6,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource DmsForegroundBrush}"/>

                <!-- WPF ComboBox nemá nativní multiselect. Používáme proto kompaktní
                     rozbalovací panel s ListBoxem a checkboxy. -->
                <ToggleButton x:Name="BtnWorkcenterPicker"
                              Width="310"
                              MinHeight="30"
                              Margin="0,0,16,0"
                              HorizontalContentAlignment="Left"
                              Padding="8,3"
                              Click="BtnWorkcenterPicker_Click">
                    <TextBlock x:Name="TxtWorkcenterSelection"
                               TextTrimming="CharacterEllipsis"
                               Foreground="{DynamicResource DmsForegroundBrush}"/>
                </ToggleButton>

                <Popup x:Name="PopupWorkcenters"
                       PlacementTarget="{Binding ElementName=BtnWorkcenterPicker}"
                       Placement="Bottom"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       Closed="PopupWorkcenters_Closed">
                    <Border Width="390"
                            MaxHeight="430"
                            Padding="10"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource DmsBorderBrush}"
                            Background="{DynamicResource DmsPanelBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0"
                                        Orientation="Horizontal"
                                        Margin="0,0,0,8">
                                <Button x:Name="BtnSelectAllWorkcenters"
                                        MinWidth="90"
                                        Margin="0,0,8,0"
                                        Padding="8,3"
                                        Click="BtnSelectAllWorkcenters_Click"/>
                                <Button x:Name="BtnClearWorkcenters"
                                        MinWidth="90"
                                        Padding="8,3"
                                        Click="BtnClearWorkcenters_Click"/>
                            </StackPanel>

                            <ListBox x:Name="LstWorkcenters"
                                     Grid.Row="1"
                                     MinHeight="80"
                                     MaxHeight="350"
                                     SelectionMode="Multiple"
                                     SelectionChanged="LstWorkcenters_SelectionChanged"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected, Mode=TwoWay}"
                                                  Padding="3"
                                                  Foreground="{DynamicResource DmsForegroundBrush}">
                                            <TextBlock Text="{Binding DisplayText}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </CheckBox>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </Popup>

                <TextBlock x:Name="LblShift"
                           Margin="0,0,6,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource DmsForegroundBrush}"/>
                <ComboBox x:Name="CmbShift"
                          Width="280"
                          Margin="0,0,16,0"
                          SelectionChanged="Filter_SelectionChanged">
                    <!-- Explicitní šablona obchází globální ComboBox styl, který
                         v některých DMS tématech zobrazil CLR název objektu. -->
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayText}"
                                       TextTrimming="CharacterEllipsis"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button x:Name="BtnRefresh"
                        MinWidth="110"
                        Margin="0,0,16,0"
                        Padding="12,5"
                        Click="BtnRefresh_Click"/>

                <CheckBox x:Name="ChkAutoRefresh"
                          VerticalAlignment="Center"
                          Checked="ChkAutoRefresh_Changed"
                          Unchecked="ChkAutoRefresh_Changed"
                          Foreground="{DynamicResource DmsForegroundBrush}"/>
            </WrapPanel>
        </Border>

        <DataGrid x:Name="GridOverview"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource DmsBorderBrush}"
                  Background="{DynamicResource DmsPanelBrush}"
                  Foreground="{DynamicResource DmsForegroundBrush}"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="ColWorkcenter" Binding="{Binding WorkcenterCode}" Width="100"/>
                <DataGridTextColumn x:Name="ColShift" Binding="{Binding ShiftName}" Width="150"/>
                <DataGridTextColumn x:Name="ColOrder" Binding="{Binding OrderCode}" Width="105"/>
                <DataGridTextColumn x:Name="ColItem" Binding="{Binding ProductCode}" Width="145"/>

                <DataGridTemplateColumn x:Name="ColState" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StateBrush}" Padding="5,2">
                                <TextBlock Text="{Binding StateName}"
                                           Foreground="{Binding StateForegroundBrush}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn x:Name="ColStateDuration" Width="125">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StateBrush}" Padding="5,2">
                                <TextBlock Text="{Binding StateDurationText}"
                                           Foreground="{Binding StateForegroundBrush}"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn x:Name="ColUserText" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StateBrush}" Padding="5,2">
                                <TextBlock Text="{Binding StateUserText}"
                                           Foreground="{Binding StateForegroundBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn x:Name="ColPlannedPerformance"
                                    Binding="{Binding PlannedPerformanceText}"
                                    Width="130"/>
                <DataGridTextColumn x:Name="ColCurrentPerformance"
                                    Binding="{Binding CurrentPerformanceText}"
                                    Width="145"/>
                <DataGridTextColumn x:Name="ColPoTarget"
                                    Binding="{Binding OrderTargetText}"
                                    Width="125"/>
                <DataGridTextColumn x:Name="ColPoGood"
                                    Binding="{Binding OrderGoodText}"
                                    Width="125"/>
                <DataGridTextColumn x:Name="ColShiftGood"
                                    Binding="{Binding ShiftGoodText}"
                                    Width="130"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,10,0,0" LastChildFill="True">
            <TextBlock x:Name="TxtRows"
                       DockPanel.Dock="Left"
                       Margin="0,0,18,0"
                       Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
            <TextBlock x:Name="TxtRefresh"
                       DockPanel.Dock="Right"
                       Margin="18,0,0,0"
                       TextAlignment="Right"
                       Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
            <TextBlock x:Name="TxtStatus"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
        </DockPanel>
    </Grid>
</UserControl>

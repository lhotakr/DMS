<UserControl x:Class="DMS.Desktop.Views.Framework.FrameworkDiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="26" FontWeight="Bold"/>
            <TextBlock x:Name="SubtitleText" Margin="0,6,0,0" TextWrapping="Wrap" Opacity="0.85"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,12">
            <Button x:Name="RunButton" Click="RunButton_Click" MinWidth="150" Padding="14,8"/>
            <TextBlock x:Name="SummaryText" Margin="16,8,0,0" FontWeight="SemiBold"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  x:Name="ResultsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="True"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" MinWidth="90"/>
                <DataGridTextColumn Header="Check" Binding="{Binding Name}" Width="220" MinWidth="160"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" MinWidth="320"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="320" MinWidth="220"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" x:Name="FooterText" Margin="0,10,0,0" TextWrapping="Wrap" Opacity="0.75"/>
    </Grid>
</UserControl>

<UserControl x:Class="DMS.Desktop.Views.SystemRoles.RoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DMS.Desktop.Views.SystemRoles"
             Background="{DynamicResource DmsBackgroundBrush}">

    <UserControl.Resources>
        <local:Sys12RoleTextConverter x:Key="Sys12RoleTextConverter"/>

        <Style x:Key="Sys12GridTextBlockStyle"
               TargetType="{x:Type TextBlock}"
               BasedOn="{StaticResource DmsDataGridTextBlockElementStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}"
                             Value="True">
                    <Setter Property="Foreground"
                            Value="{DynamicResource DmsOnAccentBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Sys12EditableGridRowStyle"
               TargetType="{x:Type DataGridRow}"
               BasedOn="{StaticResource DmsEditableGridRowStyle}">
            <Style.Triggers>
                <Trigger Property="IsSelected"
                         Value="True">
                    <Setter Property="Foreground"
                            Value="{DynamicResource DmsOnAccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">

        <StackPanel Margin="0">

            <TextBlock x:Name="TxtTitle"
                       Text="SYS12 - Role management"
                       Style="{StaticResource DmsTitleTextStyle}"/>

            <Border Style="{StaticResource DmsCardBorderStyle}"
                    Margin="0,0,0,12">

                <StackPanel>

                    <TextBlock x:Name="TxtCardTitle"
                               Text="DMS roles"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{DynamicResource DmsForegroundBrush}"
                               Margin="0,0,0,10"/>

                    <TextBlock x:Name="TxtTechnicalNote"
                               Text="Roles are used to control access to transactions. Keep role codes uppercase and without spaces."
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource DmsMutedForegroundBrush}"
                               Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,10">

                        <TextBlock x:Name="TxtFilterLabel"
                                   Text="Filter:"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"
                                   Foreground="{DynamicResource DmsForegroundBrush}"/>

                        <TextBox x:Name="TxtFilter"
                                 Width="320"
                                 Margin="0,0,8,0"
                                 TextChanged="TxtFilter_TextChanged"/>

                        <CheckBox x:Name="ChkShowInactive"
                                  Content="Show inactive"
                                  IsChecked="True"
                                  VerticalAlignment="Center"
                                  Checked="FilterChanged"
                                  Unchecked="FilterChanged"/>
                    </StackPanel>

                    <DataGrid x:Name="GridRoles"
                              Style="{StaticResource DmsDataGridStyle}"
                              RowStyle="{StaticResource Sys12EditableGridRowStyle}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="True"
                              CanUserDeleteRows="False"
                              MinHeight="420"
                              Margin="0,0,0,12">

                        <DataGrid.Columns>

                            <DataGridTextColumn x:Name="ColCode"
                                                Header="Code"
                                                Binding="{Binding Code, UpdateSourceTrigger=PropertyChanged}"
                                                Width="190"
                                                MinWidth="160"
                                                SortMemberPath="Code"
                                                ElementStyle="{StaticResource Sys12GridTextBlockStyle}"
                                                EditingElementStyle="{StaticResource DmsDataGridTextBoxEditingStyle}"/>

                            <DataGridTemplateColumn x:Name="ColName"
                                                    Header="Name"
                                                    Width="260"
                                                    MinWidth="220"
                                                    SortMemberPath="Name">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource Sys12RoleTextConverter}, ConverterParameter=Name}"
                                                   Style="{StaticResource Sys12GridTextBlockStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource DmsDataGridTextBoxEditingStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn x:Name="ColDescription"
                                                    Header="Description"
                                                    Width="*"
                                                    MinWidth="360"
                                                    SortMemberPath="Description">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource Sys12RoleTextConverter}, ConverterParameter=Description}"
                                                   Style="{StaticResource Sys12GridTextBlockStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource DmsDataGridTextBoxEditingStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridCheckBoxColumn x:Name="ColIsActive"
                                                    Header="Active"
                                                    Binding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}"
                                                    ElementStyle="{StaticResource DmsCenteredCheckBoxStyle}"
                                                    EditingElementStyle="{StaticResource DmsCenteredCheckBoxStyle}"
                                                    Width="100"
                                                    MinWidth="90"
                                                    SortMemberPath="IsActive"/>

                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">

                        <Button x:Name="BtnReload"
                                Content="Reload"
                                MinWidth="120"
                                Margin="0,0,8,0"
                                Click="BtnReload_Click"/>

                        <Button x:Name="BtnSave"
                                Content="Save"
                                MinWidth="120"
                                Margin="0,0,8,0"
                                Style="{StaticResource DmsPrimaryButtonStyle}"
                                Click="BtnSave_Click"/>

                        <Button x:Name="BtnMarkDeleted"
                                Content="Mark for deletion"
                                MinWidth="150"
                                Margin="0,0,8,0"
                                Click="BtnMarkDeleted_Click"/>

                        <Button x:Name="BtnRestoreRow"
                                Content="Restore mark"
                                MinWidth="130"
                                Click="BtnRestoreRow_Click"/>

                    </StackPanel>

                </StackPanel>
            </Border>

            <TextBlock x:Name="TxtStatus"
                       Foreground="{DynamicResource DmsMutedForegroundBrush}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>

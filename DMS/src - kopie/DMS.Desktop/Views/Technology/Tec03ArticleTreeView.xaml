<UserControl x:Class="DMS.Desktop.Views.Technology.Tec03ArticleTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="760">
    <Border Margin="0,24,0,8"
            Padding="14"
            CornerRadius="10"
            BorderThickness="1"
            Background="{DynamicResource DmsPanelBrush}"
            BorderBrush="{DynamicResource DmsBorderBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="620"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock x:Name="TxtTitle"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource DmsForegroundBrush}"/>
                <TextBlock x:Name="TxtSubtitle"
                           Margin="0,4,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
            </StackPanel>

            <WrapPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBlock x:Name="LblSuccessorDepth"
                           Margin="0,0,6,0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource DmsForegroundBrush}"/>

                <ComboBox x:Name="CmbSuccessorDepth"
                          Width="105"
                          Margin="0,0,14,0"
                          SelectionChanged="CmbSuccessorDepth_SelectionChanged"/>

                <CheckBox x:Name="ChkSiblings"
                          Margin="0,0,14,0"
                          VerticalAlignment="Center"
                          IsChecked="True"
                          Checked="ChkSiblings_Changed"
                          Unchecked="ChkSiblings_Changed"
                          Foreground="{DynamicResource DmsForegroundBrush}"/>

                <Button x:Name="BtnReload"
                        Margin="0,0,8,0"
                        Padding="12,6"
                        Click="BtnReload_Click"/>

                <Button x:Name="BtnCenter"
                        Margin="0,0,8,0"
                        Padding="12,6"
                        Click="BtnCenter_Click"/>
            </WrapPanel>

            <TextBlock x:Name="TxtHint"
                       Grid.Row="2"
                       Margin="0,0,0,8"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource DmsMutedForegroundBrush}"/>

            <Grid Grid.Row="3" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource DmsForegroundBrush}"/>
                <TextBlock x:Name="TxtSelection"
                           Grid.Column="1"
                           Margin="18,0,0,0"
                           VerticalAlignment="Center"
                           TextAlignment="Right"
                           Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
            </Grid>

            <Border x:Name="Viewport"
                    Grid.Row="4"
                    ClipToBounds="True"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{DynamicResource DmsBackgroundBrush}"
                    BorderBrush="{DynamicResource DmsBorderBrush}"
                    MouseWheel="Viewport_MouseWheel"
                    MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                    MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                    MouseMove="Viewport_MouseMove"
                    MouseLeave="Viewport_MouseLeave">
                <Canvas x:Name="GraphCanvas"
                        Width="1600"
                        Height="760"
                        Background="Transparent"/>
            </Border>

            <StackPanel Grid.Row="5" Margin="0,8,0,0">
                <TextBlock x:Name="TxtLegend"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource DmsMutedForegroundBrush}"/>
                <TextBlock x:Name="TxtWarning"
                           Margin="0,5,0,0"
                           TextWrapping="Wrap"
                           Visibility="Collapsed"
                           Foreground="#C56A43"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

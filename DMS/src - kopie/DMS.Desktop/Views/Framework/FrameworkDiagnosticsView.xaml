<UserControl x:Class="DMS.Desktop.Views.Framework.FrameworkDiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="26" FontWeight="Bold"/>
            <TextBlock x:Name="SubtitleText" Margin="0,6,0,0" TextWrapping="Wrap" Opacity="0.85"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="RunButton"
                    Grid.Column="0"
                    Click="RunButton_Click"
                    MinWidth="150"
                    Padding="14,8"/>

            <Button x:Name="CopyButton"
                    Grid.Column="1"
                    Click="CopyButton_Click"
                    MinWidth="145"
                    Margin="8,0,0,0"
                    Padding="14,8"/>

            <TextBlock x:Name="SummaryText"
                       Grid.Column="2"
                       Margin="16,8,0,0"
                       FontWeight="SemiBold"/>

            <ComboBox x:Name="StatusFilter"
                      Grid.Column="3"
                      Width="155"
                      SelectionChanged="StatusFilter_SelectionChanged"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  x:Name="ResultsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="StatusColumn"
                                    Binding="{Binding Status}"
                                    Width="110"
                                    MinWidth="90"/>
                <DataGridTextColumn x:Name="CategoryColumn"
                                    Binding="{Binding Category}"
                                    Width="150"
                                    MinWidth="120"/>
                <DataGridTextColumn x:Name="NameColumn"
                                    Binding="{Binding Name}"
                                    Width="230"
                                    MinWidth="170"/>
                <DataGridTextColumn x:Name="DetailsColumn"
                                    Binding="{Binding Details}"
                                    Width="2*"
                                    MinWidth="340"/>
                <DataGridTextColumn x:Name="SourceColumn"
                                    Binding="{Binding Source}"
                                    Width="340"
                                    MinWidth="240"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   x:Name="FooterText"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   Opacity="0.75"/>
    </Grid>
</UserControl>
